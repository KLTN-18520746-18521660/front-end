/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NAPipe } from './n-a.pipe';

describe('Pipe: NAe', () => {
  it('create an instance', () => {
    let pipe = new NAPipe();
    expect(pipe).toBeTruthy();
  });
});
