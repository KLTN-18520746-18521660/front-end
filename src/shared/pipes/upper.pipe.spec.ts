/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UpperPipe } from './upper.pipe';

describe('Pipe: Uppere', () => {
  it('create an instance', () => {
    let pipe = new UpperPipe();
    expect(pipe).toBeTruthy();
  });
});
